import logo from "../Picture/Logo.png";
import "../index.css";
export default function footer() {
    return (
        <header id="App-footer">
            <img src={logo} alt="logo" width={100} height={100} id="logo"></img>
        </header>
        )
}